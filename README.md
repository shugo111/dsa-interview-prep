# DSA Interview Prep — Interactive Guide

**Live site → https://shugo111.github.io/dsa-interview-prep/**

A single, self-contained HTML app for data-structures & algorithms interview prep. No build step, works offline, one file.

## What's inside

Three linked parts, with a sticky sidebar, live search, and scroll-spy:

1. **Pattern Flowchart** — a decision graph (Mermaid) that walks you from a problem's clues to the right pattern, plus 44 quick Java reference cards.
2. **In-Depth Patterns Guide** — 28 patterns explained *brute force → optimal*, with reusable templates, step-by-step dry-runs, and common pitfalls.
3. **LeetCode by Pattern** — the NeetCode 150/250 problem sets grouped by category, tagged with difficulty and companies, each with one fully-worked solution.

## Features

- 🔤 **4 languages** — every code example in **Java, C++, Python, TypeScript** with a one-click switcher (remembers your choice).
- 🎨 **Syntax highlighting** (inline, offline-safe).
- 🌙 **Dark mode**.
- ✅ **Progress tracking** — check off solved problems; a progress bar tracks you. Export/import your progress as JSON.
- 🔎 **Filters** — filter problems by difficulty & company, hide solved.
- ⌨️ **Keyboard nav** — `/` to search, `j` / `k` to move between sections.
- 📋 **Copy-code** buttons, collapsible sections, deep-link anchors.
- 📱 **Responsive** and **printable to PDF**.

## Notes

- Company tags reflect well-known public associations ("commonly asked at"), not live LeetCode-Premium frequency data.
- The C++/Python/TypeScript translations are derived from the Java originals; the full solutions are solid, a few loose template fragments may read less idiomatically.

## Local use

Just open `index.html` in any browser — that's it.
